import React from 'react';

const Navigation = () => {
    return(
        <nav style={{display: 'flex', justifyContent:'flex-end'}}>
            <p className='fs f2 b link dim white pa4 pointer'>Sign Out</p>
        </nav>
    );
}
export default Navigation;